# v1.6.0-han.1

## Summary

- Base upstream version: 1.6.0
- Target image: `hanops/ezbookkeeping:v1.6.0-han.1`
- Target platform: `linux/amd64`
- Release source branch: `dev`
- Release commit: `2cb2ddcdc`

## Fork Features (vs upstream 1.6.0)

Cumulative personal additions on top of upstream:

- Transaction text recognition — paste or import from clipboard, LLM recognizes and pre-fills transaction, available on both mobile (Quick Add page) and desktop (hover menu)
- Docker frontend build fixes for git-rev-sync
- Personal fork workflow: local smoke test, release notes template, progress tracker
- .gitignore updates for build artifacts and test results

## Changes Since v1.5.1-han.2

### Upstream Sync

Merged `upstream/main` (1.6.0) into `dev`. Includes MCP improvements, AI recognition enhancements, and bug fixes from upstream.

### Fixes

- Fix Docker frontend build: install git before pre-setup
- Fix Docker frontend build: init git repo for git-rev-sync
- Fix Docker frontend build: increase Node memory limit

### Docs

- Add development progress tracker
- Update progress tracker for this release
